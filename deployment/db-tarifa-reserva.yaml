apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-tarifa-reserva-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/db-tarifa-reserva-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-tarifa-reserva-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-tarifa-reserva-deployment
  labels:
    app: db-tarifa-reserva-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: db-tarifa-reserva-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: db-tarifa-reserva-deployment
        tier: database
    spec:
      containers:
        - name: db-tarifa-reserva
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: db-tarifa-reserva-data
      volumes:
        - name: db-tarifa-reserva-data
          persistentVolumeClaim:
            claimName: db-tarifa-reserva-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: db-tarifa-reserva-service
  labels:
    app: db-tarifa-reserva-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: db-tarifa-reserva-deployment
    tier: database
  type: LoadBalancer